{
  "name": "my-website",
  "version": "1.0.0",
  "description": "This project is a simple website consisting of three pages, designed with a monochrome theme. Below is an overview of the project's structure and files.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "watch": "nodemon --watch '*.html' --watch 'css/*.css' --watch 'js/*.js' server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
